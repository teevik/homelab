apiVersion: v1
kind: Service
metadata:
  name: alertmanager-ntfy
  namespace: victoria-metrics
spec:
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  selector:
    app: alertmanager-ntfy
