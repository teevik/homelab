apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: vm
    app.kubernetes.io/name: grafana
    app.kubernetes.io/version: 13.2.0
    helm.sh/chart: grafana-12.7.3
  name: vm-grafana
  namespace: victoria-metrics
spec:
  ports:
    - name: service
      port: 80
      protocol: TCP
      targetPort: grafana
  selector:
    app.kubernetes.io/instance: vm
    app.kubernetes.io/name: grafana
  type: ClusterIP
