apiVersion: v1
kind: Service
metadata:
  name: changedetection-browser
  namespace: changedetection
spec:
  ports:
    - name: ws
      port: 3000
      targetPort: 3000
  selector:
    app: changedetection-browser
