apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: crafty-config
  namespace: crafty
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: longhorn
