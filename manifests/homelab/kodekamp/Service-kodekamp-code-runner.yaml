apiVersion: v1
kind: Service
metadata:
  name: kodekamp-code-runner
  namespace: kodekamp
spec:
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  selector:
    app: kodekamp-code-runner
