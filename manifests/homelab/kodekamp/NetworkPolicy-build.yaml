apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: build
  namespace: kodekamp
spec:
  egress:
    - {}
  podSelector:
    matchLabels:
      app: build-kodekamp
  policyTypes:
    - Egress
