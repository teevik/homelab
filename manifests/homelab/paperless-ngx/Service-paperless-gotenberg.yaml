apiVersion: v1
kind: Service
metadata:
  name: paperless-gotenberg
  namespace: paperless-ngx
spec:
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  selector:
    app: paperless-gotenberg
